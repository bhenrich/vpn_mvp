Archived legacy Go/OpenVPN stack copied from `server/`.

This directory preserves the old prototype components:
- `auth` (Go)
- `openvpn` server and scripts
- Compose files for the legacy stack

Note: PKI materials were not copied; see `openvpn/pki/README.md`.


