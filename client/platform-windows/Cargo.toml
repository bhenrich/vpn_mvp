[package]
name = "platform-windows"
version = "0.1.0"
edition = "2021"

[dependencies]
vpn-core = { path = "../vpn-core" }
async-trait = "0.1"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }


