Skeleton for the Rust node agent (`wireguard-rs` integration to be added in WS5).

Build locally:

```bash
cargo build -p node-agent
```

Run container (from repo root):

```bash
docker compose up --build node-agent
```


